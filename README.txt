To run the three compartment model, and GUI copy all files in this folder to your local machine and set the Matlab working path.

You can run the GUI by simply typing "PS" in the Matlab command line.

New data can be incorporated in the form of Excel files the DATAIN directory. Ensure you use the same format as the examples in that directory. 